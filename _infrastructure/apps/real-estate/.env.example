# Real Estate App - Vite Environment Variables
# Copy to .env.local and customize

# API server URL
VITE_API_URL=http://localhost:3000

# Publishable SDK key (safe to expose in browser)
VITE_PUBLISHABLE_KEY=pk_test_demo

# Enable auth dev mode (bypasses authentication in development)
VITE_AUTH_DEV_MODE=true
