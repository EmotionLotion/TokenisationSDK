apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "compliance-service.fullname" . }}
  labels:
    {{- include "compliance-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "compliance-service.selectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.service.metricsPort }}"
        prometheus.io/path: "/metrics"
      labels:
        {{- include "compliance-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "compliance-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.service.metricsPort }}
              protocol: TCP
          env:
            - name: NODE_ENV
              value: {{ .Values.environment | quote }}
            - name: PORT
              value: {{ .Values.service.targetPort | quote }}
            - name: METRICS_PORT
              value: {{ .Values.service.metricsPort | quote }}
            - name: DATABASE_HOST
              value: {{ .Values.database.host | quote }}
            - name: DATABASE_NAME
              value: {{ .Values.database.name | quote }}
            - name: DATABASE_PORT
              value: {{ .Values.database.port | quote }}
            - name: KYC_PROVIDER
              value: {{ .Values.compliance.kycProvider | quote }}
            - name: WEBHOOK_PATH
              value: {{ .Values.compliance.webhookPath | quote }}
            - name: COMPLIANCE_INVESTOR_CAP_ENABLED
              value: {{ .Values.compliance.modules.investorCap.enabled | quote }}
            - name: COMPLIANCE_MAX_INVESTORS
              value: {{ .Values.compliance.modules.investorCap.maxInvestors | quote }}
            - name: COMPLIANCE_JURISDICTION_WHITELIST_ENABLED
              value: {{ .Values.compliance.modules.jurisdictionWhitelist.enabled | quote }}
            - name: COMPLIANCE_ALLOWED_COUNTRIES
              value: {{ .Values.compliance.modules.jurisdictionWhitelist.allowedCountries | join "," | quote }}
            - name: COMPLIANCE_LOCKUP_ENABLED
              value: {{ .Values.compliance.modules.lockupPeriod.enabled | quote }}
            - name: COMPLIANCE_LOCKUP_DAYS
              value: {{ .Values.compliance.modules.lockupPeriod.defaultDays | quote }}
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "compliance-service.fullname" . }}-secrets
                  key: databasePassword
            - name: KYC_PROVIDER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "compliance-service.fullname" . }}-secrets
                  key: kycProviderApiKey
                  optional: true
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
