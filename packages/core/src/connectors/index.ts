/**
 * SDK Connectors
 *
 * Generic external service integrations.
 * Vertical-specific connectors live in their own packages:
 * - VastAI connector → @tokenisation/compute
 * - Document signing → @tokenisation/chains
 */
