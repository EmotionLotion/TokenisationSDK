# Fix Queue

Prioritized, actionable fixes discovered by the loop harness. Severity: P0 (blocks external dev) → P3 (polish).
Status: `open` | `in-progress` | `done` | `wontfix`.

> No production code is modified during early audit stages. Items are recorded here; fixes are applied in later iterations with a matching test or doc update (master rule 14).

| ID | Sev | Stage found | Item | Status |
|----|-----|-------------|------|--------|
| F1 | P0 | repo_audit | Root README curl Quick Start returns 401 out of the box: uses `X-Dev-Org-Id` but `.env.example` ships `AUTH_DEV_MODE=false`. Fix README to either (a) tell devs to set `AUTH_DEV_MODE=true`, or (b) use a seeded `sk_test_` key. | open |
| F2 | P0 | repo_audit | Getting-started flow never explains how to obtain a real API key. Add `pnpm db:seed` (prints one-time `sk_test_…`) to README + `docs/getting-started/QUICKSTART.md`; replace placeholder `sk_test_xxx`. | open |
| F3 | P1 | repo_audit | No minimal/loyalty end-to-end example. Create `examples/loyalty-minimal/` against `@tokenisation/pack-loyalty` and/or the API server (mission target). | open |
| F4 | P1 | repo_audit | No loyalty doc. Add `docs/recipes/LOYALTY_POINTS.md`. | open |
| F5 | P2 | repo_audit | Mixed/stray lockfiles: `server/` has both `package-lock.json` and `pnpm-lock.yaml`; `sdk/`, `sdk-react/`, `ui/`, `ui-kit/` carry stray `package-lock.json`. Confirm impact in install_test; remove the non-pnpm lockfiles if they cause drift. | open |
| F6 | P2 | repo_audit | Stubbed features undocumented for users (jurisdiction US/SG, core DB transactions, KYC direct provider, Circle refunds, Azure storage). Add `docs/LIMITATIONS.md` or per-guide status notes. | open |
| F7 | P2 | repo_audit | No "use the SDK outside the monorepo" walkthrough (install/link, ESM/CJS, exports). Verify + document in sdk_consumer_test. | open |
| F8 | P3 | repo_audit | Qualify "compliant" language in README given Alpha status + mock-by-default KYC/custody/jurisdiction stubs. Revisit in security_review. | open |
| F9 | P0 | install_test | `@tokenisation/core` fails `tsc` build/typecheck: imports `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner` (S3StoragePlugin.ts) and `pg` (PostgresAdapter.ts) that are not in its package.json. **FIXED 2026-06-24:** added `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner` as optional `peerDependencies` and to `devDependencies`, and added `@types/pg` to `devDependencies` in `packages/core/package.json`. core now builds + typechecks clean (dist emitted); recursive build now progresses through core/compliance/server/pack-securities. | done |
| F11 | P1 | install_test | `pnpm --filter @tokenisation/sdk test:run` (README-advertised): 890 tests pass but 11/40 test files fail to load with `Cannot find module './cjs/react-jsx-dev-runtime.development.js'`. Root cause: React 18 vs 19 split across workspace. Pin React to one major (peerDeps + resolutions) or fix vitest jsx runtime. | open |
| F12 | P3 | install_test | Peer-dep version splits from `pnpm install`: react-native/react-dom/react-test-renderer want react@^19 (found 18.3.1); typedoc wants TS≤5.4 (found 5.9.3); ui has zod 3 vs 4 and react 18 vs 19. Align versions. Revisit in packaging_review. | open |
| F13 | P1 | install_test (post-F9) | `@tokenisation/pack-loyalty` build/typecheck fails (~497 errors) in `src/ui/AhoyBalanceWidget.tsx`: `--jsx` not enabled, missing react/lucide-react types, node16 extensionless imports (TS2834), implicit anys. Mission's target vertical. **FIXED 2026-06-24:** the `src/ui/*.tsx` React components are not re-exported from `src/index.ts` and require react+lucide-react (not package deps). Excluded `src/ui` from `packages/pack-loyalty/tsconfig.json` build, matching the existing `@tokenisation/pack-travel` convention (which also excludes `src/ui`). No product logic changed. Verified: typecheck exit 0, clean build emits index/manifest/adapters/packs/plugins/types and NO `dist/ui`. Recursive typecheck: pack-loyalty Done (and realestate now Done — its prior errors were build-order). | done |
| F14 | P1 | install_test (post-F9) | `@tokenisation/chains` typecheck fails (68 errors), e.g. `src/contracts/adapters/ERC1155Adapter.ts`: `validatedConfig is of type 'unknown'` (TS18046) — untyped zod result. **FIXED 2026-06-24:** single root cause was `zod` missing from `packages/chains/package.json` dependencies (imported in validation.ts, ChainlinkConfig.ts, DeploymentService.ts → TS2307 → `z` untyped → schema callbacks implicit-any (TS7006) → `validateOrThrow()` returns `unknown` (TS18046)). Added `"zod": "^3.22.4"` to `dependencies` (matches core/pack-loyalty). No `any`, no source changes — pure inference restored. Verified: chains typecheck + build exit 0; sdk cascade gone (456→0; sdk builds Done in topo order). | done |
| F16 | P1 | install_test (post-F14) | `@tokenisation/pack-travel` build/typecheck fails with `sh: tsc: not found`. **FIXED 2026-06-24:** sole root cause was **`typescript` missing from devDependencies** (tsc not linked). Added `"typescript": "^5.3.0"` to `packages/pack-travel/package.json` devDeps. CORRECTION to earlier hypothesis: pack-travel does NOT import chains/pack-loyalty/sdk-react in compiled code — those were comments/TODOs (`// Moved to @tokenisation/chains`, `// excluded: requires @tokenisation/sdk-react`, `// TODO: migrate to pack-loyalty`); the only real `@tokenisation/*` import is `@tokenisation/core` (already declared). No workspace-dep additions needed, no @types/node needed, no product logic changed. Verified: typecheck + build exit 0; recursive build now passes through pack-travel (next bail is the website, F10). | done |
| F18 | P0 | api_server_test | Root README token example is incomplete and fails at runtime: `POST /api/v1/tokens` requires an **`Idempotency-Key` header** (400 `IDEMPOTENCY_KEY_REQUIRED`) AND a **`totalSupply`** field (400 `VALIDATION_ERROR: totalSupply: Required`). README shows only `{name,symbol,chainId,assetId}`. The canonical `docs/api/REST_API.md` is CORRECT — fix is to align the README Quick Start (and the SDK snippet) to it, and pair with F1 (AUTH_DEV_MODE). | open |
| F19 | P2 | api_server_test | Committed OpenAPI specs are stale: `server/openapi.json` and `server/openapi.yaml` contain only **6 paths** (all `/investors/*`), while the live runtime spec `GET /api/openapi.json` has **311 paths**. A dev generating a client from the checked-in spec gets a near-empty API. Regenerate via `pnpm openapi:generate` and commit; add a CI check to keep them in sync. | open |
| F20 | P2 | api_server_test | Data-linking: `POST /api/v1/tokens` accepted `assetId` but persisted `assetId: null` (link dropped). **FIXED 2026-06-24:** root cause was `createToken()` in `server/src/services/token.service.ts` — the `db.insert(tokens).values({...})` listed every column EXCEPT `assetId` (the `tokens` table already has an `asset_id` FK column → `assets.id`). Added `assetId: input.assetId` to the insert (one line; schema/validation unchanged). Added server regression test `server/src/__tests__/token-assetid.test.ts` (2 tests: persists supplied assetId + round-trip; null when omitted; passes). Verified end-to-end via SDK: token created with `asset.id` returns `token.assetId === asset.id`. (Sub-note: asset create returning `orgId: null` under dev-header is separate/minor and not addressed here.) | done |
| F21 | P1 | sdk_consumer_test | **SDK↔server contract bug:** `client.assets.create()` returns `{asset:{...}}` (server envelope) but is typed `Promise<Asset>`, so `result.id` is `undefined` at runtime. **FIXED 2026-06-24:** added an `unwrapAsset()` helper in `packages/core/src/modules/assets.ts` and applied it in `create()` and `get()` (both server endpoints return `{asset}`; `update()` already returns the bare asset so untouched). Helper is defensive (returns `body.asset ?? body`) and does not hide errors (HTTP layer throws first). Server response behavior unchanged (per scope). Added regression test `sdk/tests/AssetsResponseContract.test.ts` (3 tests, pass). Verified end-to-end: README-style `const asset = await client.assets.create(...); asset.id` returns a real id, `client.assets.get(asset.id)` round-trips, token created with `asset.id`. core+sdk typecheck+build pass. NOTE F21b still open: `assets.list()` server shape is `{assets,total,page,limit}`, not the SDK's `PaginatedResponse<{data}>` — separate, deferred. | done |
| F21b | P2 | sdk_consumer_test | `client.assets.list()` returns `{assets,total,page,limit}` from the server but the SDK types it `PaginatedResponse<Asset>` (expects `{data,...}`), so `.data` is undefined. Align the list envelope (SDK unwrap or server shape). Deferred (not the create-flow blocker). | open |
| F22 | P0 | sdk_consumer_test | **`@tokenisation/core` not consumable as published.** (a) `npm install` → ERESOLVE (react@^18 vs react@19 via optional drizzle peers). (b) root barrel statically re-exported React components (`./components`) and the Postgres adapter (`./storage` → drizzle/pg), so `import { createApiClient }` crashed with `Cannot find package 'react'/'drizzle-orm'` in clean apps. **FIXED 2026-06-24:** (1) removed `export * from './components/index.js'` from `packages/core/src/index.ts` (kept only the framework-agnostic `defaultTheme`/`createStyles`/`TokenisationTheme` from `./components/theme.js`, which has no React import, for backwards compat with realestate); (2) changed root storage export from `./storage/index.js` to `./storage/DatabaseAdapter.js` (drops the drizzle/pg PostgresAdapter from root); (3) added `package.json` subpath exports `./components`, `./storage`, `./storage/postgres` so the functionality is preserved; (4) widened optional peer `react` to `^18.0.0 || ^19.0.0` to clear the ERESOLVE. VERIFIED: a fresh external app installed the packed core tarball with **no `--legacy-peer-deps` and no react/drizzle/pg** (40 pkgs), then `import { createApiClient }` worked and created an asset + token. core+sdk typecheck+build all pass. No functionality deleted (moved to subpaths). | done |

---

## Loop interpretation (2026-06-24)

Mission = **SDK product readiness**, not full-monorepo perfection. The **SDK/server developer path is GREEN** after F9, F13, F14, F16: every SDK package + `sdk` + `sdk-react` + `sdk-react-native` (build) + `server` + `ui-kit` + `create-tokenised-asset` + `sdk-playground` builds AND typechecks clean. `install_test` is **completed** for the SDK product path (`install_test_sdk_path=completed`); full-monorepo health is tracked separately (`install_test_full_monorepo=blocked`). The items below **do not block `api_server_test`**.

## Full monorepo health / later (deferred — does NOT block SDK stages)

| ID | Sev | Stage found | Item | Status |
|----|-----|-------------|------|--------|
| F10 | P2 | install_test | `_infrastructure/website` (docusaurus) build fails: expects docs at `../docs` but website was moved under `_infrastructure/`. Repoint Docusaurus path to repo `docs/` or exclude website from default `pnpm -r run build`. Currently makes the literal `pnpm -r run build` exit non-zero. Off the SDK/server developer path. | deferred |
| F15 | P2 | install_test | `sdk-react-native` `typecheck` script Fails with **no `error TS` output** (same `tsc: not found` class as F16; missing `typescript` devDep). It **BUILDS Done**. Likely fix: add `typescript` devDep (mirror F16). Off the core SDK build path. | deferred |
| F17 | P2 | install_test | App builds fail (OFF the SDK/server developer path, like website F10): `ui` (pkg `@tokenisation/console`) and `_infrastructure/apps/real-estate` both fail `pnpm build`. Fix or exclude from the default recursive build. Not required for SDK consumers. | deferred |
| — | info | install_test (post-F16) | After F16, the ENTIRE SDK/server path builds AND typechecks green: chains, compliance, compute, core, pack-loyalty, pack-securities, pack-supply-chain, pack-travel, realestate, sdk, sdk-react, sdk-react-native(build), server, ui-kit, create-tokenised-asset, sdk-playground. The only remaining recursive failures are the deferred items above (F10/F15/F17). Per the loop interpretation decision, these do not block advancing to api_server_test. | n/a |
